declare type StripePaySession = {
  id: string;
};

declare type GlobalStateProps = {
  token: string;
};

declare type PostItem = {
  name: string;
  description: string;
};

declare type GetItem = {
  id: number;
  name: string;
  description: string;
};
